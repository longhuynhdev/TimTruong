# Neon PostgreSQL connection string (Npgsql key-value format, NOT the URI format)
# Get host/database/username from your Neon project dashboard → Connection Details
NEON_CONNECTION_STRING=Host=ep-xxx.us-east-2.aws.neon.tech;Database=neondb;Username=user;Password=password;SSL Mode=Require;Channel Binding=require

# CORS — your frontend domain
FRONTEND_URL=https://timtruong.app

